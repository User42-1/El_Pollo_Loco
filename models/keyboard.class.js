// Stores which key is pressed 
// - is stored only ones in games.js (not in every instance)
// - adds eventListener (for keydown, keyup, ...)

class Keyboard {
    LEFT = false;
    RIGHT = false;
    UP = false;
    DOWN = false;
    SPACE = false;
    D = false;
}